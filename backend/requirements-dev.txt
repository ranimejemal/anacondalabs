# AegisLab backend — extra dependencies for running the test suite only.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# (not needed to run the app itself, only to run `pytest backend/`)
-r ../requirements.txt
pytest==8.2.2
pytest-asyncio==0.23.7
